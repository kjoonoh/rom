import React from 'react';
import { View, Text } from 'react-native';

const SearchScreen = () => {
    return (
        <div>
            search
        </div>
    );
};

export default SearchScreen;